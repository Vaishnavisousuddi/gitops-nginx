apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-site.fullname" . }}-test-connection"
  labels:
    {{- include "nginx-site.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginx-site.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
